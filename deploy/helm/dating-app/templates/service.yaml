apiVersion: v1
kind: Service
metadata:
  name: {{ include "dating-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "dating-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
